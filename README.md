# Airflow Data Engineering Demo

A simple Airflow DAG pipeline that demonstrates extract-transform workflow using mocked data.

Learning Airflow basics.

## Features

- Uses `PythonOperator`
- Fully offline (mocked API data)
- Works with official Airflow Docker Compose setup
- No external dependencies needed